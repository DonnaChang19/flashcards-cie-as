name: keep-alive
on:
  schedule:
    - cron: '13 4 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase (keep free project alive)
        run: curl -s -o /dev/null -w "%{http_code}" "https://nhceunbblvyoepzdjrny.supabase.co/rest/v1/decks?select=slug&limit=1"
